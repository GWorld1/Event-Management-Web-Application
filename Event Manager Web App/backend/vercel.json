{
    "version": 2,
    "builds": [
      {
        "src": "api/**",
        "use": "@vercel/node",
        "config": {
          "maxLambdaSize": "5mb"
        }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/$1"
      }
    ]
  }